[project]
name = "spotify"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "curl-cffi>=0.15.0",
    "fastapi[standard]>=0.136.0",
    "requests>=2.33.1",
    "uvicorn>=0.44.0",
]
